# kanonサイト 仕様書

最終更新：2026-08-12

## 1. 目的
1ページのWebページから、訪問者を fans サイトへ誘導する。
いわゆる「リンクまとめページ（リンクインバイオ）」。RINA・kotoha と同じ形。

## 2. 想定する使い方
- SNSのプロフィール欄にこのページのURLを貼る
- 訪問者はこのページを経由して、各 fans サイトへ移動する
- 流入の大半はスマホ

## 3. 基本情報
| 項目 | 内容 |
|------|------|
| 対象 | 個人（kanon さん） |
| 誘導先 | fans サイト 2〜3件 |
| デザイン | 色っぽい・セクシー系 |
| 素材 | あり（受け取り待ち） |

## 4. ページ構成
| ブロック | 内容 |
|---------|------|
| 言語切替バー | 8言語（日英中韓西伊越比）。ページ最上部に固定 |
| ヘッダー | アイコン画像（丸・グロー） / 名前「kanon」 / 一言プロフィール |
| リンクボタン群 | fans サイトへのボタンを縦に並べる（1件1ボタン） |
| 注意書き | 18歳未満向けの注記（不要なら削除可） |
| フッター | コピーライト |

## 5. 誘導先リンク一覧
| # | 表示名 | URL | 状態 |
|---|-------|-----|------|
| 1 | OnlyFans | https://onlyfans.com/kanon_nippon | ✅ 設置済み |
| 2 | myfans | https://myfans.jp/kanon_nippon | ✅ 設置済み |

- 並び順は海外向けを優先して OnlyFans → myfans
- ボタンの補足表示は両方とも `@kanon_nippon`
- サービス名は固有名詞なので翻訳しない（言語を切り替えても表示は同じ）

## 5.5 言語ごとにリンクを出し分ける
特定の言語のときだけボタンを表示／非表示にできる。HTMLのタグに属性を1つ足すだけ。

| 属性 | 動き |
|------|------|
| `data-only-lang="ja"` | 日本語のときだけ表示 |
| `data-only-lang="ja,ko"` | 日本語と韓国語のときだけ表示（カンマ区切り） |
| `data-hide-lang="ja"` | 日本語のときだけ隠す |

書き方の例：
```html
<a class="link-btn" href="https://example.com" target="_blank"
   rel="noopener noreferrer" data-only-lang="ja">
```

- `index.html` の誘導ボタンの下に、**日本語専用ボタンの雛形をコメントアウトで置いてある**。
  URL と表示名を入れてコメントを外せばすぐ使える。
- ボタン以外（注意書き・プロフィール文など）にも同じ属性が使える。
- 表示切替はJS側で `style.display` を直接操作している。
  CSSに `display` を書き足すと崩れる原因になるので触らないこと。

## 6. 素材リスト
| 素材 | 用途 | 配置場所 | 状態 |
|------|------|---------|------|
| アイコン画像（正方形・500px以上） | ヘッダー | `images/icon.jpg` | ✅ 設置済み（line_oa_chat_260812_123533.jpg） |
| 背景画像（任意・縦長） | ページ全体 | `images/bg.jpg` | 受け取り待ち |
| プロフィール文（1〜2行） | ヘッダー | index.html 内 | 受け取り待ち |

※ 素材が入るまではCSSのグラデーションで代用（画像なしでも成立するデザイン）。

## 6.5 多言語対応
海外向けのため8言語に対応。魚種別説明ページと**同じ8言語・同じ並び順・同じ仕組み**。

| 順 | 言語 | コード |
|---|------|-------|
| 1 | 日本語 | ja |
| 2 | English | en |
| 3 | 中文 | zh |
| 4 | 한국어 | ko |
| 5 | Español | es |
| 6 | Italiano | it |
| 7 | Tiếng Việt | vi |
| 8 | Tagalog | tl |

- **初期表示**：端末の言語に自動で合わせる（対応外の言語は英語）。魚種別ページは常に日本語スタートだが、こちらは海外向けなので自動判定にしている
- **翻訳テキストの場所**：`index.html` 末尾の `var I18N = { ... }`
- **翻訳の追加方法**：HTMLの要素に `data-i18n="キー名"` を付け、`I18N` の各言語に同じキーを足す
- **現在翻訳しているもの**：プロフィール文 / リンクのラベルと補足 / 18歳未満の注意書き
- **翻訳していないもの**：名前「kanon」（全言語共通）

## 6.8 日本向けファン案内ページ（jp/）
日本のファン向けの案内ページを別ページとして作成。

| 項目 | 内容 |
|------|------|
| 場所 | `jp/index.html` + `jp/guide.css` |
| URL | https://arlemcompany-lang.github.io/kanon/jp/ |
| 言語 | 日本語のみ（言語切替バー無し） |
| 入口 | トップページ下部の `⇒ 別の応援方法` というテキストリンク（`data-only-lang="ja"`）。**日本語表示のときだけ出る**。fansサイトのボタンと区別するため、四角いボタンにはしていない |

**中身の構成**
1. はじめに（あいさつ）
2. 2つのサイトについて（myfans / OnlyFans の比較表）
3. 登録のしかた（4ステップ）
4. お願い（転載禁止など）
5. よくある質問（開閉式）
6. 応援ボタン（日本の方向けなので myfans を上に）

**現状**：枠だけ完成。文章は仮。HTML内の `<!-- ▼ ... -->` が記入待ちの箇所。
詳しくは `jp/CLAUDE.md` を参照。

## 7. デザイン方針
- **雰囲気**：色っぽい・セクシー・大人っぽい
- **ベース**：白（ほんのり赤みのある白へグラデーション）
- **アクセント**：ワインレッド ／ ローズ ／ ゴールド
- **フォント**：明朝系（Noto Serif JP）＋ 英字はセリフ体、字間広め
- **動き**：ボタンに光が横切るシマー、ゆっくり動く背景グロー（控えめ）

## 8. 進捗
- [x] フォルダ作成
- [x] CLAUDE.md / kanonサイト.md 作成
- [x] index.html + css/style.css 作成（ダミーリンク）
- [x] fans サイトの URL 差し替え
- [x] アイコン写真の差し込み
- [ ] 背景写真の差し込み（任意）
- [ ] プロフィール文の確定
- [x] 多言語対応（8言語）
- [x] 日本向けファン案内ページ（jp/）の枠組み
- [ ] 案内ページの文章記入（あいさつ・料金・FAQ）
- [x] GitHub Pages で公開

## 8.5 公開URL
**https://arlemcompany-lang.github.io/kanon/**

| 項目 | 内容 |
|------|------|
| リポジトリ | https://github.com/arlemcompany-lang/kanon （public） |
| ブランチ | main / ルート |
| 更新方法 | ファイルを編集 → `git add -A` → `git commit` → `git push` （1〜2分で反映） |

※ `<meta name="robots" content="noindex">` を入れているため検索には出ない（URLを知っている人だけが見られる）。

## 9. 未確定リスト（ユーザー確認待ち）
1. アイコン・背景の写真データ
3. プロフィール文（1〜2行）
4. 18歳未満向けの注記を入れるか

## 10. 今後の拡張メモ
「後ほど少し複雑にする」予定あり。想定される拡張：
- ページ追加（プロフィール詳細 / ギャラリー / お問い合わせ）
- 写真ギャラリー（スライダー）
- 年齢確認モーダル
→ CSSは `css/style.css` に分離済みなので、ページを増やしても流用できる。
